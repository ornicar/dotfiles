# Use "less -r" as my pager                                                                                                                                        
--pager=less -r

# Ignore some dirs
--ignore-dir=cache
--ignore-dir=logs
--ignore-dir=target
--ignore-dir=web/js
--ignore-dir=web/css
--ignore-dir=tags

# Vim like case sensitivity
--smart-case

# Twig type
--type-set=twig=.twig

# Jade type
--type-set=jade=.jade
