# Use "less -r" as my pager                                                                                                                                        
--pager=less -r

# Ignore some dirs
--ignore-dir=cache
--ignore-dir=logs

# Vim like case sensitivity
--smart-case
