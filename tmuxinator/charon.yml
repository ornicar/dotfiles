# ~/.tmuxinator/charon.yml

project_name: charon
project_root: ~/charon
tabs:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/charon && bin/play
        - cd ~/charon && vim
  - shell: cd ~/charon
