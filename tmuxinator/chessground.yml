# ~/.tmuxinator/chessground.yml

project_name: chessground
project_root: ~/chessground
windows:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/chessground && lein cljsbuild auto dev
        - cd ~/chessground && vim
  - shell: cd ~/chessground
