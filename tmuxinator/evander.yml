# ~/.tmuxinator/evander.yml

project_name: evander
project_root: ~/jirafe/evander
tabs:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/jirafe/evander && sbt
        - cd ~/jirafe/evander && vim
  - shell: cd ~/jirafe/evander
