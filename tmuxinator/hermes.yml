# ~/.tmuxinator/hermes.yml

project_name: hermes
project_root: ~/jirafe/hermes
tabs:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/jirafe/hermes && sbt
        - cd ~/jirafe/hermes && vim
  - shell: cd ~/jirafe/hermes
