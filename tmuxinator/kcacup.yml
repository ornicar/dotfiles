# ~/.tmuxinator/kcacup.yml

project_name: kcacup
project_root: ~/kcacup
tabs:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/kcacup && play
        - cd ~/kcacup && vim
  - shell: cd ~/kcacup
