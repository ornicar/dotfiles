# ~/.tmuxinator/lila.yml

project_name: lila
project_root: ~/lila
windows:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/lila && bin/play
        - cd ~/lila && vim
  - shell: cd ~/lila
