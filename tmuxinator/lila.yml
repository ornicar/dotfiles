# ~/.tmuxinator/lila.yml

project_name: lila
project_root: ~/lila
tabs:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/lila && bin/dev
        - cd ~/lila && vim
  - shell: cd ~/lila
