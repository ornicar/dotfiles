# ~/.tmuxinator/nestor.yml

project_name: nestor
project_root: ~/nestor
tabs:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/nestor && bin/play
        - cd ~/nestor && vim
  - shell: cd ~/nestor
