# ~/.tmuxinator/pcg.yml

project_name: pcg
project_root: ~/pcg
windows:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/pcg 
        - cd ~/pcg && vim
  - shell: cd ~/pcg
  - shell: cd ~/pcg
