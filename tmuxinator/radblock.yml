# ~/.tmuxinator/radblock.yml

project_name: radblock
project_root: ~/radblock
tabs:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/radblock && bin/play
        - cd ~/radblock && vim
  - shell: cd ~/radblock
