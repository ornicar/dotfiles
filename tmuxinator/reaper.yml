# ~/.tmuxinator/reaper.yml

project_name: reaper
project_root: ~/jirafe/reaper
tabs:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/jirafe/reaper && sbt
        - cd ~/jirafe/reaper && vim
  - shell: cd ~/jirafe/reaper
