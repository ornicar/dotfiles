# ~/.tmuxinator/reaper.yml

project_name: reaper
project_root: ~/reaper
tabs:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/reaper && sbt
        - cd ~/reaper && vim
  - shell: cd ~/reaper
