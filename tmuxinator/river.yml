# ~/.tmuxinator/river.yml

project_name: river
project_root: ~/scalariver
tabs:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/scalariver && sbt
        - cd ~/scalariver && vim
  - shell: cd ~/scalariver
