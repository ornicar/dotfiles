# ~/.tmuxinator/sacem.yml

project_name: sacem
project_root: ~/sacem
tabs:
  - main: 
      layout: even-horizontal
      panes: 
        - cd ~/sacem && sbt
        - cd ~/sacem && vim
  - shell: cd ~/sacem
